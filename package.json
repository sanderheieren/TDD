{
  "name": "test-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose"
  },
  "devDependencies": {
    "vite": "^2.5.4"
  },
  "dependencies": {
    "jest": "^27.2.1"
  }
}
